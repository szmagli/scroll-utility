{
    "scripts": {
        "assets": "cp -r assets dist",
        "orgmode": "emacs --iconic --no-splash -nw README.org -f org-html-export-to-html --kill && mv README.html dist/index.html",
        "build": "rm -rf dist && tsc index.ts --outFile dist/index.js",
        "doc": "npm run build && cp node_modules/scroll-utility/dist/scroll-utility.js dist && npm run orgmode && npm run assets"
    },
    "dependencies": {
        "scroll-utility": "file:../"
    },
    "devDependencies": {
        "typescript": "^3.5.3"
    }
}
